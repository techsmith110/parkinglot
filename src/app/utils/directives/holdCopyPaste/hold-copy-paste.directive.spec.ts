import { HoldCopyPasteDirective } from './hold-copy-paste.directive';

describe('HoldCopyPasteDirective', () => {
  it('should create an instance', () => {
    const directive = new HoldCopyPasteDirective();
    expect(directive).toBeTruthy();
  });
});
